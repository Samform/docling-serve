version: v1.1.0
steps:
  - build: -t docling-serve-cu124:git-submodule-1 -f Containerfile . --context https://github.com/Samform/docling-serve#update-local-changes
    name: BuildDoclingYml
    # arg: "--build-arg UV_SYNC_EXTRA_ARGS='--no-group pypi --group cu124'"
    env:
      - DOCKER_BUILDKIT=1
