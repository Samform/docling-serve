version: v1.1.0
steps:
  - build: -t {{.Run.Registry}}/docling-serve-cu124:git-submodule-1
      -f Containerfile
      --platform linux/amd64
      --build-arg UV_SYNC_EXTRA_ARGS='--no-group pypi --group cu124'
    env:
      - name: DOCKER_BUILDKIT
        value: "1"
